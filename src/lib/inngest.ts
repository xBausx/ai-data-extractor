// src/lib/inngest.ts

import { Inngest } from 'inngest'

// This is the main Inngest client.
// It is used to define functions and send events.
export const inngest = new Inngest({ id: 'adept-ai-data-extractor' })
